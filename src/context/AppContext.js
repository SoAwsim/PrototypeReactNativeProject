import { createContext } from "react";

export const AuthContext = createContext(); // used for managing the authentication flow
export const LocaleContext = createContext(); // used for managing and getting the state of locale
export const ThemeContext = createContext(); // used for getting and setting the theme
