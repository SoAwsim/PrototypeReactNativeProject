import { View } from "react-native";

// dummy splash screen for now maybe add app icon or a loading indicator in the future
export default function SplashScreen() {
    return (
        <View style={{ flex: 1 }} />
    )
}